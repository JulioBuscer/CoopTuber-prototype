interface HTMLVideoElement {
    captureStream?(frameRate?: number): MediaStream;
}

type blendshapesCategory = {
    index: number,
    score: number;
    categoryName: string;
    displayName: string
}
